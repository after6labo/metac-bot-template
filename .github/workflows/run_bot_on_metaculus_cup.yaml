name: Forecast on Metaculus Cup (disabled for zero-cost experiment)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Metaculus Cup is outside this experiment and its paid defaults are disabled."
          exit 1
